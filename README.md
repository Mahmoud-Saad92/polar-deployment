# `Bazinga (BZA)` Docker
A public repository includes docker containers definitions required for polar bookshop

### Pre-Requisites:
#### — Create an external docker-network with name [polar-postgres-nw]

Run to build docker container locally

```bash
$ DOCKER_BUILDKIT=1 docker compose up -d
```
